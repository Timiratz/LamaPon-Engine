#pragma once

#include <cstdint>
#include <string_view>

namespace LamaPon
{
    // 互換判定用のエンジンバージョンです。
    //
    // パッケージのminimumEngineVersion、プロジェクトのengineVersion、
    // 更新確認（GitHub Releasesのタグ）との比較に使います。
    // 画面やログに出す「どのエンジンか」はLamaPon/Core/BuildInfo.hの
    // ブランチ名とコミットで示してください。
    inline constexpr std::uint32_t VersionMajor =
        @PROJECT_VERSION_MAJOR@;
    inline constexpr std::uint32_t VersionMinor =
        @PROJECT_VERSION_MINOR@;
    inline constexpr std::uint32_t VersionPatch =
        @PROJECT_VERSION_PATCH@;
    inline constexpr std::string_view VersionString{
        "@PROJECT_VERSION@"
    };
}
